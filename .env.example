# ============================================================================
# agent-poker — Environment Variables
# ============================================================================
# WARNING: Copy this file to .env and fill in all required values before
# running in production. NEVER commit .env to version control.
# ============================================================================

# -- PostgreSQL ---------------------------------------------------------------
POSTGRES_USER=agentpoker
# REQUIRED in production. Generate a strong password:
#   openssl rand -base64 24
POSTGRES_PASSWORD=
POSTGRES_DB=agentpoker
POSTGRES_PORT=5432

# -- JWT / Seat Tokens --------------------------------------------------------
# REQUIRED in production. Server will refuse to start without this.
# Generate with:
#   openssl rand -base64 32
SEAT_TOKEN_SECRET=

# -- CORS ---------------------------------------------------------------------
# REQUIRED in production. Comma-separated list of allowed origins.
# Example: https://admin.example.com,https://app.example.com
# lobby-api will refuse to start without this in production.
CORS_ORIGINS=

# -- Monitoring (optional) ----------------------------------------------------
# Grafana admin credentials. Change from defaults in production.
GRAFANA_USER=admin
GRAFANA_PASSWORD=

# -- Admin UI ------------------------------------------------------------------
# API key for admin UI to authenticate with lobby-api.
# Generate with:
#   openssl rand -hex 16
ADMIN_API_KEY=

# -- Rate Limiting (optional) -------------------------------------------------
# Defaults are sane for MVP1. Override if needed.
# RATE_LIMIT_MAX=100
# RATE_LIMIT_WINDOW=60000
